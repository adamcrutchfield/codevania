/// @description Insert description here
// You can write your code in this editor

// Inherit the parent event
event_inherited();

//activate only if recieving color 80+% red
active = compareColors(arrLight, 1, 0, 0) >= 0.8;